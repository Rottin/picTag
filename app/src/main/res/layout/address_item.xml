<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="5dp"
    android:paddingTop="5dp" >

    <RadioButton
        android:id="@+id/address_item_bt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="20dp"
        android:focusable="false" />

    <TextView
        android:id="@+id/address_item_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/address_item_bt"
        android:text="姓名"
        tools:ignore="HardcodedText" />

    <TextView
        android:id="@+id/address_item_phone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/address_item_name"
        android:layout_toRightOf="@id/address_item_bt"
        android:text="电话"
        tools:ignore="HardcodedText" />

    <TextView
        android:id="@+id/address_item_addr"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/address_item_phone"
        android:layout_toRightOf="@id/address_item_bt"
        android:text="地址"
        tools:ignore="HardcodedText" />

</RelativeLayout>